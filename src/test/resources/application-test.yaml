spring:
  datasource:
    url: jdbc:tc:postgresql:17.5:///integration-test
    username: user
    password: password
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    locations: classpath:test/migration
  data:
    jdbc:
      repositories:
        enabled: true
